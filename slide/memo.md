# HTMLプレビュー（ブラウザ自動起動）
`$ npx @marp-team/marp-cli slide/slides.md --preview`

# PDF出力
`$ npx @marp-team/marp-cli slide/slides.md --pdf --output slide/slides.pdf`

# PPTX出力
`$ npx @marp-team/marp-cli slide/slides.md --pptx --output slide/slides.pptx`
